export enum FormCallType {
    kLogin,
    kRegister
}

/**
 * Overview of all the current pet states. Stored
 * in enum as to make future expansion possible.
 */
export enum PetState {
    kAnnoyed,
    kAsleep,
    kLove,
    kNeutral,
    kSad
}